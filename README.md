#include <stdio.h>#include <stdlib.h>Struct Node {Int coach;Struct Node* next;};Int main() {Struct Node *head, *second, *third;Head = (struct Node*)malloc(sizeof(struct Node));Second = (struct Node*)malloc(sizeof(struct Node));Third = (struct Node*)malloc(sizeof(struct Node));Head->coach = 1;Head->next = second;Second->coach = 2;Second->next = third;Third->coach = 3;Third->next = NULL;Struct Node* temp = head;Printf(“Train Coach List:\n”);While(temp != NULL) {3Printf(“Coach %d -> “, temp->coach);Temp = temp->next;}Printf(“NULL”);Return 0;}OUTPUT Train Coach List:Coach 1 -> Coach 2 -> Coach 3 -> NULL
